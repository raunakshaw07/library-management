const endpoint = "http://127.0.0.1:8000"

export const users = endpoint + "/users/user";
export const books = endpoint + '/books/book';
export const transaction = endpoint + "/transactions/transaction";
